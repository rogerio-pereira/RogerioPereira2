name: lint-reusable

on:
  workflow_call:
    inputs:
      mode:
        required: true
        type: string

jobs:
  lint:
    runs-on: ubuntu-latest
    environment: Testing
    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'

      - name: Install Dependencies
        run: |
          composer install -q --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist
          npm install

      - name: Run Pint (fix mode)
        if: inputs.mode == 'fix'
        run: vendor/bin/pint --parallel

      - name: Run Pint (check mode)
        if: inputs.mode == 'check'
        run: vendor/bin/pint --parallel --test

      - name: Commit Changes
        if: inputs.mode == 'fix'
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'style(api/ui): Fix code style (pint and lint)'
